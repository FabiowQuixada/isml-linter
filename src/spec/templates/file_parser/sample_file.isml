
<isset name="pageUrl" value="${URLUtils.https('Reorder-ListingPage')}" scope="page"/>
<div class="addToCartUrl" style="display: none;">${addToCartUrl}</div>

// This rule is disabled, so will not go into the output file;
<isscript>

</isscript>
