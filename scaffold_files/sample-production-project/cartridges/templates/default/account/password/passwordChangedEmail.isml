<body>

    <h1>${Resource.msg('msg.passwordemail.dear','login',null)}&nbsp;${pdict.firstName}&nbsp;${pdict.lastName},</h1> 

    <p>${Resource.msg('msg.passwordchangedemail.message1','login',null)}</p>   
    
    <p>${Resource.msg('msg.passwordchangedemail.message2','login',null)}</p>
    
    <p><a href="${pdict.url}">${pdict.url}</a></p> 
    
    <p>${Resource.msg('msg.passwordchangedemail.message3','login',null)}</p>

</body>
